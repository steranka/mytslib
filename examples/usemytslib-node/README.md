# Sample code that uses mytslib library
This directory contains a trivial node program that uses the mytslib library.

# How to run this

Build the package and run the program

```
npm install
npm run app
```

Or you could run the sample using: ```node main.js```
